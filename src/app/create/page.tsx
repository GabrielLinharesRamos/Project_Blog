"use client"

import Editor from "@/components/Editor";

export default function EditPage() {
  return (
    <div
      style={{
        height: "85vh",
        display: "flex",
        justifyContent: "center",
      }}
    >
      <Editor />
    </div>
  );
}